# Week 2026-W39 — Plan

_Generated 2026-09-21 (Monday) by automotive-skills-daily-standup (PLAN mode)._

## Recap — past 7 days

```
eed2159 auto(triage): empty board, untyped-issue flag traced to PLAN template, pycache audit closed
e58d22a auto(release): cut v2026.09.W38, second two-week tag, four pairs repaired
54e46b3 auto(docs): W37+W38 changelog rolled, four example READMEs, five missed runs reconciled
9170271 auto(polish): dia reviewer misread joint reviews, scope and combined RACI cells
a2af2f1 auto(polish): cs-goals-builder read TARA by header name, goal text and asset no longer swapped
a8f430d auto(polish): hw-safety-reqs overrides silently dropped on TSR ID drift, now applied and warned
```

**There was no W38 plan.** The last PLAN run was W37 on 09-07; W38's Monday slot was one of the
five that did not fire in the last ten working days. So this plan inherits W37's commitments
directly, two weeks late, and the first thing it owes is an answer on whether it honored them.

W37 wrote down two consequences and asked the next plan to be held to them:

1. **The sysml JSON-input batch was deferred to W38.** It was taken in neither W38 nor here. See
   *Carried* below — it is now twice-deferred and that is a decision, not a slip.
2. **"W38 takes at least one target by rule (c) from outside the modeling cluster."** Honored.
   This week is cyber and cross-cutting infrastructure; there is no sysml or mbse target on the
   table for the first time in four planning cycles.

The three polish runs that did fire (09-16 to 09-18) all landed reviewer-side or chain-side column
defects. That pattern is what shaped this week's table.

## Where the targets came from

- **Rule (a) — open issues: no input.** The board was at zero this morning. It reads zero because a
  human closed all 15 on 09-11, not because the work landed; the 09-19 and 09-20 journals both say
  so at length.
- **Rule (b) — orphan builders: no input.** 76/76 paired, alias registry honored, fifth consecutive
  week with nothing here.
- **Rule (c) — least-recently-touched: 21 builders still sit on 2026-05-01**, 69 of 76 are stale.

So the table is built from the **eight pre-diagnosed candidates in section 4 of
`docs/triage/2026-09-20.md`** — items already found and written up in polish logs but never filed,
which is the actual reason the board reads empty. Filing them was named as this plan's highest-value
half-hour in two consecutive journal entries. Four are filed today (#62-#65); the remaining four are
listed under *Carried*.

## Audit run before slotting — one HIGH resized

Target 2's source log says the `_find_header_row` >=3-cell rule "hides **every** two-column tab from
its reviewer." That is a suite-wide claim carrying a HIGH severity, and W37's lesson is that a cheap
claim gets measured before it gets a slot. Scan across all 152 archives:

| Finding | Count |
|---|---|
| Reviewers defining `_find_header_row` | 24 of 76 |
| Of those, gating on `>= 3` | **24 of 24** — one threshold literal, zero variants |
| Paired builders with a demonstrable exactly-two-column header assignment | **2** |
| Builders constructing a two-item header row via `.append([...])` | 0 |

The two live cases are `cs-concept-builder` (`Reference` / `Source`) and `sw-fmea-builder`
(`SWC Type` / `Failure Mode`).

**The defect is real but it is uniform-and-latent, not widespread-and-active.** The fix is one
literal in 24 archives; today's symptom is two tabs. It keeps its slot — a silent false-pass in 24
reviewers is worth one cheap day — but it is scoped as a mechanical change plus a regression sweep,
not as a week-eating repair. Resizing it before Wednesday is what stops it swallowing the day.

## This week's targets (3 + 1 fold-in)

| # | Issue | Skill(s) | Domain | Definition of done |
|---|---|---|---|---|
| 1 | [#62](https://github.com/jherrodthomas/automotive-skills-suite/issues/62) | cs-goals-builder | cyber | Map STRIDE to `Cybersecurity_Property` per row instead of writing a constant `Confidentiality`, and state the mapping in SKILL.md. Verified by a build whose output carries at least 3 distinct property values, plus the negative assertion: **an input with no Information Disclosure threat must produce zero `Confidentiality` rows.** That assertion is what proves the hardcode is gone rather than merely overwritten. |
| 2 | [#63](https://github.com/jherrodthomas/automotive-skills-suite/issues/63) | 24 reviewers (`_find_header_row`) | cross-cutting | Lower the gate to `>= 2` in all 24. Verified on the two measured cases (`cs-concept`, `sw-fmea`) returning a real FC/PC verdict where they returned NO/NA. **Mandatory regression sweep:** run all 24 reviewers over their builders' output and confirm no check that returned FC now returns NO — loosening a header heuristic can match an earlier row by accident, and that is this change's actual risk. |
| 3 | [#64](https://github.com/jherrodthomas/automotive-skills-suite/issues/64) | `scripts/chain_contract_audit.py` | ci | Add column-presence-and-naming assertions to every chain edge already audited. Acceptance test is fixed in advance: **the audit must reproduce the `cs-concept` `02_CS_Goals_Echo` shift as a failure**, since that edge passes clean today. Regenerate `docs/chain-contract-audit.md`. Scope is presence and naming only — no value-level validation. |
| — | [#65](https://github.com/jherrodthomas/automotive-skills-suite/issues/65) | cs-concept-builder | cyber | Fold-in, not a day. Realign the `02_CS_Goals_Echo` header/data offset. **Ordering constraint: do not fix it before target 3 can watch it fail.** It is target 3's only known-bad edge; fixing it first costs the audit its acceptance test. |

Slotting: **Tue -> #62. Wed -> #63. Thu -> #64, with #65 closed behind it.**

#62 goes first because it is the highest-severity single-skill defect on the table and the shortest
path to a green day. #63 is Wednesday because the resizing above turned it into mechanical work with
a well-defined regression sweep — the right shape for a middle day. #64 is Thursday because it is
the highest-leverage item in the repo and has been carried three weeks running; putting it last
contradicts W34's lesson, and the mitigation is that #65 gives it a concrete pass/fail the moment
it is written, so it cannot end the day ambiguous.

Three targets, not five. Every prior week that slotted four or more carried at least one.

## Domain-spread check

Current inventory (machine-derived from today's STATUS table, not carried forward):

```
safety 15 . quality 10 . comms 8 . cyber 6 . autosar 5 . diagnostics 5 . program-mgmt 5
v&v 5 . aspice 4 . sysml 4 . calibration 3 . mbse 3 . sotif 3
```

W35 autosar+sysml . W36 v&v+sysml+mbse . W37 v&v+mbse+comms . **W39 cyber + cross-cutting**.

The four-week modeling-cluster run is broken, which was W37's explicit instruction. But the
underlying imbalance has not moved and should not be read as fixed: **21 builders still carry
2026-05-01** — safety 9, quality 7, cyber 3, sotif 2 — and quality (10 builders, all 10 stale) and
comms (8 builders, all 8 stale) have not been entered since W30-W33. Two of this week's three
targets are cross-cutting infrastructure, which touches every domain's reviewers but advances no
domain's builder.

**Held to next plan: one target from the quality or comms 2026-05-01 cohort, by rule (c), chosen on
staleness rather than on a polish log.** Filing pre-diagnosed items is the right move while a
backlog of them exists; it is also a rule that will keep selecting cyber and infrastructure forever,
because that is where the last month's polish attention went. The backlog is down to four after
today. When it empties, rotation has to drive again.

## Carried, not slotted

**From the section-4 handoff, filed nowhere yet (4 of 8 remain):**

- Builder-output-through-reviewer-probe regression harness (`ci`, HIGH) — the mechanism that would
  have caught all seven W36 reviewer crashes *and* would make target 2's regression sweep a command
  instead of a manual pass. Deliberately not filed today: it overlaps target 3's window and filing
  two infrastructure items into one week is how both get half-done.
- Repack `traceability-matrix-checklist-reviewer.skill` without `__pycache__` (`skill-bug`, MED) —
  settled as a one-archive repack artifact by the 09-20 scan, not a hygiene problem. Genuinely small.
- sysml JSON-input batch (`skill-bug`, MED) — **twice deferred, W37 -> W38 -> here.** Recording the
  reason plainly: it is an enhancement with a proven pattern (#57 left `_g()` / `write_rows()` to
  reuse) and no defect behind it, and it is in the cluster this week was told to leave. It should
  either be scheduled as an enhancement week or dropped as a standing item; carrying it a third
  time is the worst of the three options.
- `_suggest_cal()` vs the SKILL.md heuristic (`description-quality`, LOW).

**Structural, still unslotted:**

- The 24 reviewers whose `check_definitions.py` carries neither `Must` nor `Shall` — where does
  obligation actually live? Open unknown since W37, not a known bug. Note this is the *same count*
  as target 2's 24 gated reviewers; whether they are the same 24 is worth five minutes and has not
  been checked.
- `regen_status.py` -> `max(builder, reviewer)` for `Last Touched`, so a reviewer-only repair moves
  its row. One line, still deliberately deferred: STATUS is a contract two other modes read.
- `scripts/` invariant lint (five invariants, the fifth being *every reviewer's
  `generate_checklist.py` runs to completion on its builder's `{}` output*).
- `examples/mbse-model-architecture-builder/` and `examples/mbse-requirements-allocation-builder/`
  still lack `sample_input.json`.
- Standing item 9 — advertised vs shipped check counts, roughly 100 advertised checks that do not
  exist across at least 8 reviewers.

## For the human

- **One taxonomy decision unblocks a finding that has recurred four Sundays running.** 26 of 51
  issues have no type label; all 26 are PLAN-generated `weekly-target` issues, and none of the seven
  mandated types describes "polish this builder." Either add a `polish` type or exempt
  `weekly-target` from needing one. Section 1 of `docs/triage/2026-09-20.md` has the full diagnosis.
  *Partial mitigation applied today without pre-empting the decision:* #62, #63 and #65 carry
  `skill-bug` and #64 carries `ci`, because all four are genuine defect items. The types fit here;
  they will not fit the next plan that slots a pure freshness rotation.
- **Two tags are pushed and unpublished:** `v2026.09.W36` (three weeks) and `v2026.09.W38`. Review
  `RELEASES.md` and click Publish.
- **#60 and #61 were closed `completed` on 09-11 and never worked.** Reopen them or say they are
  abandoned; they are still being cited as carried work in the polish logs.
- **`examples/sample_dia_esc.json` dual-Accountable semantics** needs a human call before it can be
  filed as work — a RACI row with two Accountable parties is either a fixture bug or a deliberate
  OEM/Tier-1 shared-accountability case, and the answer changes what the reviewer should assert.
- **Duplicate v&v labels** — `v-and-v` (in use) and `vv` (unused). Delete one; autonomous runs will
  not merge labels.
- **The scheduling gap is still the repo's largest recurring problem.** Five missed slots in ten
  working days, on top of the 08-27 outage, and W38 lost its plan entirely — which is why this
  document is dated two weeks after the commitments it is answering.
