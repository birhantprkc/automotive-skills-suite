# Week 2026-W33 — Plan

_Generated 2026-08-10 (Monday) by automotive-skills-daily-standup (PLAN mode)._

## Recap — past 7 days

```
01bb5f0 auto(triage): label audit and DoD evidence check on seven open issues, STATUS regen
56c841c auto(release): v2026.08.W32 weekly snapshot, RELEASES and CHANGELOG roll, STATUS regen
cd6a29e auto(docs): W32 changelog roll, three example stubs, STATUS regen
9b4660e auto(polish): safety-program-risk-register docs made honest, dangling reference removed
6fab2ce auto(polish): a2l-builder members normalization and conversion_methods schema doc fix
f00f1b2 auto(polish): aspice-process-evidence tabs 06/07/08 wired to real input data
fcc4c0b auto(plan): W32 carries five open targets, no new issues, STATUS regen
```

W32 hit all three of its polish days and landed real changes: `aspice-process-evidence-builder`
tabs 06/07/08 wired to real input data (finishing the #45 bundle), an `a2l-builder` members
normalization plus a schema doc fix (clearing the residual #47 findings), and a docs-honesty pass
on `safety-program-risk-register-builder` (#49). Sunday's triage then did something more useful
than labelling: it checked every open issue's definition of done **against the tree** rather than
against issue comments, and found the open count is inflated.

Net position entering W33: **four issues (#44, #45, #47, #49) are complete and open only because
autonomous runs never close issues.** Three items carry real work: #43, #46, and the stub tail of
#48. That is down from a five-deep carry-over at the start of W32 — the queue shrank for the first
time in four weeks.

## This week's targets (4)

Priority order applied: (a) open-issue-referenced skills fill the first three slots; (b) no orphan
builders exist (0 red, alias registry honored per `docs/PAIRING_ALIASES.md`), so the orphan rule
was skipped; (c) least-recently-touched selection filled the fourth slot, which is the first time
this year it has been reached.

| # | Issue | Skill(s) | Domain | Definition of done |
|---|---|---|---|---|
| 1 | [#48](https://github.com/jherrodthomas/automotive-skills-suite/issues/48) (carried x2, stub tail only) | sotif-analysis-builder | sotif | `examples/sotif-analysis-builder/README.md` created against the real generator output (tab count and sheet names taken from the .skill, not invented). Polish log already exists from the 2026-06-09 W24 pass. This is the only remaining DoD item; #48 is closable after it. |
| 2 | [#43](https://github.com/jherrodthomas/automotive-skills-suite/issues/43) (carried x4) | cs-architecture-builder (reader) | cyber | `cs_concept_reader.py` rewritten against the tab names cs-concept-builder actually emits (`05_CSR_Catalog` / `06_CAL_Allocation`, not `02_CSRs_Catalog` / `03_CAL_Allocations`); SKILL.md Step 1 updated; smoke test over a real cs-concept output yields nonzero CSRs; archive re-zipped. |
| 3 | [#50](https://github.com/jherrodthomas/automotive-skills-suite/issues/50) (new) | cdd-builder | diagnostics | Frontmatter validated, generator smoke-tested, pairing with `cdd-checklist-reviewer` confirmed, `docs/skill-polish-log/cdd-builder.md` created, `examples/cdd-builder/README.md` present. |
| 4 | [#46](https://github.com/jherrodthomas/automotive-skills-suite/issues/46) (carried x4) | (repo-wide, read-only) | chain-break | Audit script + `docs/chain-contract-audit.md` committed comparing emitted vs. expected sheet names across all builder→reviewer and builder→builder handoffs; one issue per confirmed break; the audit itself modifies no `.skill` file. |

Three POLISH days (Tue/Wed/Thu) against four targets. Recommended slotting: **Tue → #48 then #50**
(the #48 tail is a single stub file and should not consume a whole day; #50 is the natural
same-day companion), **Wed → #43** (largest, needs a real fixture and has now been carried four
weeks — it gets the full day or it gets descoped), **Thu → #46**. #46 is the expected carry into
W34 if Wednesday overruns.

**Why a new issue was opened this week when W32 opened none.** W32's own note set the condition:
"if the carry-over queue is still five deep at the end of W32, the honest move for W33 is to cut
targets rather than carry them again." The queue is three deep, not five, and one of those three
(#48) is a ten-minute stub. Adding one target restores a full week of work without growing WIP
past capacity — and it is the only way the chronic domain-spread miss below ever gets addressed.

## Domain-spread check

Targets span **sotif, cyber, diagnostics, and chain-break (repo-wide)** — four slots, three
distinct domains plus one cross-cutting audit. This is the **first spread change since W30**.
Diagnostics enters the table for the first time since W27.

Still unworked since W27: **autosar, mbse, sysml, v&v**. Every builder in all four still shows a
2026-05-01/02 last-touched date:

| Domain | Builders | Oldest / all last-touched |
|---|---|---|
| autosar | 5 | 2026-05-02 (all) |
| mbse | 3 | 2026-05-01/02 |
| sysml | 4 | 2026-05-01/02 |
| v&v | 5 | 2026-05-02 (all) |

If a W33 target closes early, the freed slot should go to `autosar-bsw-config-builder`
(autosar, 2026-05-02) — largest untouched domain, and BSW config is the one most likely to have
drifted against the composition and RTE-mapping builders that #46's audit will touch anyway.

## Notes

- STATUS regenerated this run: 76 builders / 76 reviewers / 100% paired (2 alias pairings),
  green 12 / yellow 64 / red 0. Identical to 08-09 — no `.skill` file was touched over the weekend.
- The yellow count stays pinned in the sixties because "last touched" is git mtime on the `.skill`
  archive. The proposed freshness-by-polish-log rule change (first raised W30) is still awaiting
  human sign-off and remains the single change that would make STATUS reflect real review coverage
  rather than archive churn.
- A stale `/tmp/automotive-work` tree from an earlier run was not removable by this run (root-owned
  `.git` objects), so the clone was made to a timestamped sibling directory instead. Harmless, but
  if it recurs the fixed `WORK` path in the task file should become a timestamped one.

## Standing items for the human

1. **Close #44, #45, #47, #49** — all four have their definition of done met and verified against
   the tree (see `docs/triage/2026-08-09.md`). They are inflating the open count and skewing the
   priority-(a) rule that fills this table.
2. **Publish the pushed tags** — v2026.07.W29, v2026.07.W30, v2026.08.W31, v2026.08.W32 are pushed
   but no GitHub Release object exists (by design; autonomous runs never publish).
3. **Decide the label taxonomy** — `vv`/`v-and-v`, `bug`/`skill-bug`, `documentation`/`docs` are
   near-duplicate pairs; consolidating requires deleting labels, which is out of autonomous scope.
   Also: #48 and #49 are scheduled maintenance passes, not defects, and no existing type label fits
   them — a `polish-target` label would close that gap.
4. **Sign off (or reject) the freshness-by-polish-log STATUS rule** — outstanding since W30.
5. **Look at the vanished-issues pattern #33–#42** — flagged W30, still unexplained.
