# Triage snapshot — 2026-08-16 (Sunday autonomous TRIAGE run)

Open issues: **8**. Label mutations applied this run: **0**. Stale-issue comments posted: **0**
(no open issue has been quiet for 30+ days; oldest touch is #46 / #43 at 2026-07-20 — 27 days).

## Headline: the open count is 8, the real backlog is 1

Seven of eight open issues now meet their definition of done. Two of the three items carried out
of last Sunday's triage were closed out by this week's POLISH runs (#43 on 2026-08-12, the #48
stub tail on 2026-08-11). **#46 is the only genuinely open work item in the repo.**

## Label audit

| # | Title (short) | Type label | Domain label | Action |
|---|---|---|---|---|
| 50 | Polish cdd-builder | — | diagnostics ✅ | none — needs human triage |
| 49 | Polish safety-program-risk-register-builder | — | program-mgmt ✅ | none — needs human triage |
| 48 | Polish sotif-analysis-builder | — | sotif ✅ | none — needs human triage |
| 47 | Polish a2l-builder | skill-bug ✅ | calibration ✅ | none needed |
| 46 | Repo-wide chain-contract audit | chain-break ✅ | — (repo-wide) | none — no single domain applies |
| 45 | ASPICE bundle ignored-arg + scaffold wiring | skill-bug ✅ | aspice ✅ | none needed |
| 44 | Strip trailing NUL bytes from cs-concept generator | skill-bug ✅ | cyber ✅ | none needed |
| 43 | Repair cs-concept → cs-architecture chain break | chain-break ✅ | cyber ✅ | none needed |

**Needs human triage (below the 80% confidence bar, deliberately left unlabelled):** #48, #49, #50.
All three are scheduled *polish targets*, not reports of a confirmed defect. None of the seven
permitted type labels (`skill-bug`, `reviewer-finding`, `description-quality`, `new-skill`, `docs`,
`ci`, `chain-break`) describes "scheduled maintenance pass on a never-polished skill". This is the
**second consecutive week** the same gap has blocked labelling, and it now affects every
weekly-target issue that PLAN mode opens. Adding a `polish-target` (or `maintenance`) label to the
taxonomy would clear it permanently — that is a human decision, not an autonomous one.

**#46** remains genuinely cross-domain (it audits every builder→builder read in the suite), so no
domain label was invented for it.

## Definition-of-done evidence check

Verified against the tree and against the `.skill` archives, not against issue comments.

| # | DoD status | Evidence |
|---|---|---|
| 43 | **Met** (new this week) | `cs_concept_reader.py` now resolves `CSR_SHEETS = ("05_CSR_Catalog", "02_CSRs_Catalog")` and `CAL_SHEETS = ("06_CAL_Allocation", "03_CAL_Allocations")` — preferred name first, legacy alias retained as fallback. Repaired in `b9c8aef`. |
| 44 | **Met** | `generate_cs_concept.py` — 15,753 bytes, **0** trailing NUL bytes. Re-verified this run. |
| 45 | **Met** | Polish logs present for all four aspice builders. |
| 46 | **OPEN** | `docs/chain-contract-audit.md` absent; no audit script committed. **Carried 5 weeks, zero starts.** |
| 47 | **Met** | `docs/skill-polish-log/a2l-builder.md` + `examples/a2l-builder/README.md` both present. |
| 48 | **Met** (new this week) | The missing `examples/sotif-analysis-builder/README.md` was added 2026-08-11 in `386191e`. Last week's "Partial" is resolved. |
| 49 | **Met** | Polish log 2026-08-06; `examples/safety-program-risk-register-builder/README.md` present. |
| 50 | **Met** | Polish log 2026-08-11 walks the DoD item by item — frontmatter 413 chars, generator smoke-tested to 12 tabs, `cdd-checklist-reviewer` pairing confirmed real, example README written from the verified run. |

## Workflow defect found this run (needs a human decision)

**The 30-day stale-comment rule does not consult DoD status.** #43 and #46 were both last touched
2026-07-20 and cross the 30-day threshold on **2026-08-19** — before next Sunday's run. Under the
rule as written, next week's TRIAGE will post *"this issue has been quiet for 30+ days. Closing in
7 days unless updated"* on **#43**, whose work is complete, verified, and shipped in `v2026.08.W33`.
That comment would be actively misleading to anyone reading the issue.

The rule measures *issue-thread silence* when what matters is *work-not-started*. #46 deserves the
nudge; #43 does not. Two ways to fix it, both human calls:

1. Close the seven DoD-met issues (the standing follow-up), which makes the problem moot.
2. Amend the rule to skip the stale comment when the DoD evidence check returns **Met**.

Absent either, the stale rule will start generating noise against completed work from next Sunday.

## Label taxonomy note (unchanged, second week)

Near-duplicate pairs still fragment filtering: `vv` / `v-and-v`, `bug` / `skill-bug`,
`documentation` / `docs`. The second of each pair is the one this workflow uses. Consolidating
means deleting labels, which is out of scope for an autonomous run.
