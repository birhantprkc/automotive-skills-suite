# Triage snapshot — 2026-08-09 (Sunday autonomous TRIAGE run)

Open issues: **7**. Label mutations applied this run: **0**. Stale-issue comments posted: **0**
(no open issue has been quiet for 30+ days; oldest touch is #46 / #43 at 2026-07-20).

## Label audit

Every open issue already carries a correct domain label, and five of seven carry a type label.

| # | Title (short) | Type label | Domain label | Action |
|---|---|---|---|---|
| 49 | Polish safety-program-risk-register-builder | — | program-mgmt ✅ | none — needs human triage |
| 48 | Polish sotif-analysis-builder | — | sotif ✅ | none — needs human triage |
| 47 | Polish a2l-builder | skill-bug ✅ | calibration ✅ | none needed |
| 46 | Repo-wide chain-contract audit | chain-break ✅ | — (repo-wide) | none — no single domain applies |
| 45 | ASPICE bundle ignored-arg + scaffold wiring | skill-bug ✅ | aspice ✅ | none needed |
| 44 | Strip trailing NUL bytes from cs-concept generator | skill-bug ✅ | cyber ✅ | none needed |
| 43 | Repair cs-concept → cs-architecture chain break | chain-break ✅ | cyber ✅ | none needed |

**Needs human triage (confidence below the 80% bar, so deliberately left unlabelled):** #48 and #49
are weekly *polish targets*, not reports of a confirmed defect. None of the available type labels
(`skill-bug`, `reviewer-finding`, `description-quality`, `new-skill`, `docs`, `ci`, `chain-break`)
describes "scheduled maintenance pass on a never-polished skill". A `maintenance` or `polish-target`
label would fit; creating a new taxonomy label is a human call, not an autonomous one.

**#46** is genuinely cross-domain (it audits every builder→builder read in the suite), so no domain
label was invented for it.

## Definition-of-done evidence check

The open count is inflated. Verified against the tree rather than against issue comments:

| # | DoD status | Evidence |
|---|---|---|
| 44 | **Met** | `cs-concept-builder/scripts/generate_cs_concept.py` — 15,753 bytes, **0** trailing NUL bytes |
| 45 | **Met** | polish logs present for all four aspice builders (gap-analysis, improvement-plan, process-evidence, assessment) |
| 47 | **Met** | `docs/skill-polish-log/a2l-builder.md` + `examples/a2l-builder/README.md` both present |
| 49 | **Met** | polish log written 2026-08-06; `examples/safety-program-risk-register-builder/README.md` present |
| 48 | **Partial** | polish log exists (2026-06-09, W24 first pass) but `examples/sotif-analysis-builder/README.md` is **missing** — the DoD explicitly requires the stub |
| 46 | **Open** | `docs/chain-contract-audit.md` absent; no audit script committed |
| 43 | **Open** | `cs-architecture-builder/scripts/cs_concept_reader.py` still reads `02_CSRs_Catalog` / `03_CAL_Allocations` / `04_Threat_Mapping`; concept builder still emits `05_CSR_Catalog` / `06_CAL_Allocation` |

**Honest carry into W33: three items — #43, #46, and the stub tail of #48.** Four issues (#44, #45,
#47, #49) are complete and open only because autonomous runs never close issues.

## Label taxonomy note (human decision)

The label set contains near-duplicate pairs that will fragment future filtering:
`vv` / `v-and-v`, `bug` / `skill-bug`, `documentation` / `docs`. The second of each pair is the one
this workflow uses. Consolidating means deleting labels, which is out of scope for an autonomous
run — flagged here for a human pass.
