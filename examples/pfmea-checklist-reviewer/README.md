# pfmea-checklist-reviewer — example

**What this skill produces:** Technical reviewer for PFMEA xlsx (AIAG-VDA 2019); verifies header block completion, process flow/function mapping, S/O/D ratings, action priority computation, high-AP closure, special characteristics flagging, and control plan linkage.

**Typical input shape:** A PFMEA xlsx — normally `pfmea-builder` output, best-effort on other PFMEA-shaped workbooks.

**Expected output:** `<name>_checklist.xlsx` with every check rated FC / LC / PC / NO / NA, or left `Pending` with an `(Auto-suggest)` draft where reviewer judgment is required. The source workbook is never modified.

**Sample I/O:**

```bash
python scripts/generate_checklist.py pfmea.xlsx output_checklist.xlsx
python scripts/recalc.py output_checklist.xlsx      # optional; detects formula errors
```

Paired builder: `pfmea-builder`.

_Stub generated by the W39 Friday DOCS run (2026-09-25) — this pair was touched this week (#63 header-gate fix) but had no example README. Not yet verified by execution; a fuller writeup with real check counts and sample output belongs to a future POLISH pass on this pair._
